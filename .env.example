OPENAI_API_KEY=sk-your-key-here
# optional configuration:
MOM_MODEL=openai:gpt-4o
MOM_TMUX_SESSION=llm-mom
MOM_TMUX_WINDOW=main
MOM_POLL_SECS=0.8
MOM_TAIL_LINES=160
MOM_MAX_TRANSCRIPT=200

# Poll logic configuration
MOM_DEFAULT_WAIT_SEC=15.0  # Default sleep time when no wait_cmd provided
MOM_IDLE_THRESHOLD=3.0     # Continuous seconds of no pane change to consider idle
MOM_SPIN_MAX_SEC=45.0      # Safety cap for idle detection spin